using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerController : MonoBehaviour
{

    public int number = 5; // Integers
    public float den = 4.5f; // Floats / Decimals

    public bool condition = true; // Booleans

    public string words = "hello world"; // Strings

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
